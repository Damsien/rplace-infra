apiVersion: batch/v1
kind: CronJob
metadata:
  name: rplace
  namespace: rplace
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: rts
            image: harbor.k8s.inpt.fr/rplace/rts:v1
            imagePullPolicy: IfNotPresent
            env:
            - name: MARIADB_HOST
              value: "{{ .Values.mariadb.HOST }}"
            - name: MARIADB_PORT
              value: "{{ .Values.mariadb.PORT }}"
            - name: MARIADB_USER
              value: "{{ .Values.mariadb.USER }}"
            - name: MARIADB_PASSWORD
              value: "{{ .Values.mariadb.PASSWORD }}"
            - name: MARIADB_DATABASE
              value: "{{ .Values.mariadb.DATABASE }}"
            - name: MARIADB_DEV
              value: "{{ .Values.mariadb.DEV }}"
            - name: REDIS_HOST
              value: "{{ .Values.redis.HOST }}"
          - name: rtr
            image: harbor.k8s.inpt.fr/rplace/rtr:v1
            imagePullPolicy: IfNotPresent
            env:
            - name: REDIS_HOST
              value: {{ .Values.redis.HOST }}
          restartPolicy: OnFailure