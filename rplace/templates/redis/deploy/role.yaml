apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: modify-pods
  namespace: rplace
rules:
  - apiGroups: ["", "discovery.k8s.io"]
    resources:
      - pods
      - endpointslices
    verbs:
      - get
      - list
      - delete
      - update
      - patch